import { ApplicationConfiguration } from "#interfaces/application.interface";

/**
 * The application configuration for the `BillingApplication`.
 */
export const appConfig: ApplicationConfiguration = {
    
};